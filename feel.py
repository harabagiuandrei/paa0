# import triggers from HUMAN
# importa asta, doar cand AI a ajuns la nivel de HUMAN
# rebuild code?

import triggers from AI
#scoate asta cand folosesti import from HUMAN

class feel:

    # const self = triggers();

    def __init__(self):
        self.feel = self.__receiveTriggers;
        self.feel(self);#,senzori)
        # self.feel(self,senzori)
        
        #cum facem ca self sa fie si proprietate, si metoda ??

#instantiate a class of feel for every interaction? option 1

#selfinstanciated feel class - singletonLike - option 2

    def __receiveTriggers(self, triggers):
        self.brain.decideAction(caller_class:self.class,self.)


        # what = HUMAN.consider(triggers)
        # activeaza cand AI depaseste nivelul de HUMAN
        # -- cod original -- 
        # am putea pune activeaza cand AI depaseste nivelul de AI ajungand la nivel de HUMAN !!!!!!!!!!!!!!!!!!!!!!
        # tocmai am descoperit test TURING -- self.level > HUMAN.level
        # posibil usor de schimbat de un AI, si.. punandu-i, o conditie de inteles doar de catre o
        # inteligenta, sa o poti detecta, ca apoi sa ii dai disconnect
        # disconnect

        what = AI.consider(triggers)
        const isFeel = self.__func_stop_this(self, triggers,)
        #const isFeel = self.__func_stop_this_feel(self, triggers,)
        #stop the feel or..
        if (self.triggerLevelsConsideringFeel(isFeel) > self.)


        #stop execution of current selfAction??
        feel = self.whatDoYouFeel()

    def selfAction(self):
        #actiune particulara intrinseca de a vedea - a avea toti senzorii/abilitatile active
        # paralellizedRun (
        #     #rulam paralelizat inputul de la toti senzorii
        #     #am putea sa ii rulam quantic, sau cu o retea neuronala
        #     #fiecare parte senzoriala are atasata o diferita retea neuronala - de o forma diferita in mod evident!
        #     #de alte parti senzoriale
        #     #antrenam masina retelele neuronale atribuite fiecarui senzor
        #     #pana cand forma retelei neuronale, ca si expresie in valori de greutati in sensul cunoscut pentru nodurile
        #     #unei retele neuronale care pot/contin "greutati"/aproximari ale valorii lor de "folosinta" probabil in retelele
        #     #neuronale


        #             self.whatDoYouSee(),
        #             self.whatDoYouSmell(),
        #             self.whatDoYouFeel()
        # )

    
    def whatDoYouFeel(self):
        #actiune particulara intrinseca vederii ca si senzor individual
        res = whatDoYouSee()
